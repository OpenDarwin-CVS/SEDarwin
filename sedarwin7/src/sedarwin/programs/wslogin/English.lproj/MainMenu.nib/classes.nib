{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {"ui_ok" = id; }; 
            CLASS = LabelChooser; 
            LANGUAGE = ObjC; 
            OUTLETS = {list = NSBrowser; window = NSPanel; }; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}